{
  "name": "Toys Store Design",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "json-server": "json-server --watch db.json --port 3000",
    "start": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
